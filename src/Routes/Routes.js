const home = "/";
const signup = "/signup";
const signin = "/signin";
const comments = "/comments/:clothId";
const profile = "/profile/:userId";

export const ROUTES = {
  home: home,
  signUp: signup,
  signIn: signin,
  comments: comments,
  profile: profile
}
